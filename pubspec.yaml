name: app
description: A sample command-line application.
version: 1.0.0
# repository: https://github.com/my_org/my_repo

environment:
  sdk: ^3.1.1

# Add regular dependencies here.
dependencies:
  # path: ^1.8.0
  http: ^0.13.3

dev_dependencies:
  lints: ^2.0.0
  test: ^1.21.0
  flutter_dotenv: ^5.0.2
  

executables:
  console_app: bin/metodo_main.dart